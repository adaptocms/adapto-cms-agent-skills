{
  "name": "@adaptocms/agent-skills",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Agent skills for operating Adapto CMS end-to-end via the adapto CLI (Claude Code).",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "validate": "tsx scripts/validate-skills.ts",
    "validate:json": "tsx scripts/validate-skills.ts --json",
    "typecheck": "tsc --noEmit",
    "test": "npm run validate && npm run typecheck && node tests/smoke.mjs"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.14.0",
    "js-yaml": "^4.1.0",
    "tsx": "^4.16.0",
    "typescript": "^5.5.0"
  }
}
